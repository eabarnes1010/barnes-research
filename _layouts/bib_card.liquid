---
---
{%- comment -%}
  Minimal jekyll-scholar template: emit ONLY the title, for compact contexts like
  the home-page Publications card. Used via `{% bibliography --template bib_card %}`.
{%- endcomment -%}
{{ entry.title }}
